name: Refresh free news feed

on:
  schedule:
    - cron: '0 * * * *'
    - cron: '30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: news-feed-refresh
  cancel-in-progress: false

jobs:
  refresh-news-feed:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate news feed JSON
        run: python scripts/refresh_news_feed.py

      - name: Show feed status
        run: |
          ls -lh news_feed.json || true
          echo "Last 5 lines of news_feed.json (if present):"
          tail -n 5 news_feed.json || true

      - name: Commit updated feed
        run: |
          if git diff --quiet -- news_feed.json; then
            echo "No feed changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add news_feed.json
          git commit -m "Update news feed [skip ci]"
          git push
